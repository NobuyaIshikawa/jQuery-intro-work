$(document).ready(function(){
	// クローン用の変数を定義
	var cloned_target;

	// Q.「コピー」ボタンを押した時、変数cloned_targetにtargetクラスの内容をコピーする
	$("#copy").click(function(){

	})
	// Q.「貼り付け」ボタンを押した時、クローンした内容を<body>の先頭にコピーする
	$("#paste").click(function(){

	});
});